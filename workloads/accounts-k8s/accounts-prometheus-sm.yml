apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: accounts-app
  namespace: default
  labels:
    team: default
spec:
  selector:
    matchLabels:
      app: accounts
  endpoints:
    - targetPort: 8081
      path: /management/prometheus
